- 工作流文件名采用小写连字符命名法（kebab-case），如 `character-extract.dify.yml`，确保跨平台标识符的一致性。
- LLM 节点的系统提示词中强制要求结构化输出（如 JSON 对象或特定标记的文本块），并明确禁止使用 Markdown 代码块包裹，以方便下游程序正则解析。
- 视觉风格与角色描述字段采用机器可读的键值对格式（如 `视觉风格：...`），且字段标签保持语言无关性（始终为中文），确保解析逻辑的稳定性。