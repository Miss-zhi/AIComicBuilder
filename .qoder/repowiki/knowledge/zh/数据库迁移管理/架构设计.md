- 采用 Drizzle ORM 的迁移机制，通过 `meta/_journal.json` 维护迁移版本索引与执行顺序。
- 使用纯 SQL 文件（如 `0000_chemical_tyrannus.sql`）定义架构变更，支持创建表、添加列及外键约束。
- 配合 JSON 快照文件（如 `meta/0000_snapshot.json`）记录每个版本的完整数据库状态，用于一致性校验与回滚支持。
- 迁移策略以增量演进为主，从基础的项目、镜头、角色表结构逐步扩展至分镜、音频、AI 代理等复杂业务字段。