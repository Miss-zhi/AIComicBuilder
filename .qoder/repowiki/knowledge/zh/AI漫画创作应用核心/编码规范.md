- API路由中统一使用`getUserIdFromRequest`或`assertProjectOwnership`进行身份验证与资源访问控制。
- 服务端组件通过`next-intl/server`的`getTranslations`获取国际化文本，客户端组件使用`useTranslations` Hook。
- 数据库查询普遍采用`eq(projects.userId, userId)`模式确保数据隔离，防止跨用户数据泄露。