- 所有 API 路由均遵循 RESTful 风格并嵌套于 `src/app/api/projects/[id]/...` 路径下以实现项目级权限隔离。
- 数据库 schema 定义集中于 `src/lib/db/schema.ts`，并通过 `drizzle-kit` 管理增量迁移文件。
- AI 提示词逻辑按功能模块拆分至 `src/lib/ai/prompts/` 目录，并通过注册表模式进行统一管理。